# FSD Linting Rules 🛡️

Этот документ описывает правила линтинга, настроенные в проекте для поддержки архитектурной методологии **Feature-Sliced Design (FSD)**.

Правила реализованы с помощью `eslint-plugin-boundaries`, `eslint-plugin-import` и `eslint-plugin-simple-import-sort`.

---

## 1. Сортировка импортов (Import Sorting) 📦

Импорты автоматически сортируются и группируются в определенном порядке для улучшения читаемости. Порядок групп сверху вниз:

1.  **Node.js встроенные модули** (например, `fs`, `path`)
2.  **React и внешние библиотеки** (например, `react`, `next`, `clsx`)
3.  **Внутренние слои FSD** (в строгом порядке):
    - `@app`
    - `@pages`
    - `@widgets`
    - `@features`
    - `@entities`
    - `@shared`
4.  **Относительные импорты из корня** (`src/`)
5.  **Родительские директории** (`../`)
6.  **Текущая директория** (`./`)
7.  **Стили** (`.css`, `.scss`, `./styles`)

> **Зачем:** Это позволяет сразу видеть структуру зависимостей в файле и отделять внешние библиотеки от внутреннего кода.

---

## 2. Границы слоев (Layer Boundaries) 🚧

Главное правило FSD: **"Импортировать можно только с нижележащих слоев"**.

Линтер строго контролирует, какой слой может импортировать другой.

| Слой (кто импортирует) | Разрешено импортировать (откуда)            | Описание                                           |
| :--------------------- | :------------------------------------------ | :------------------------------------------------- |
| **App**                | Все слои                                    | Собирает все приложение, инициализирует провайдеры |
| **Pages**              | `widgets`, `features`, `entities`, `shared` | Страницы собираются из виджетов и фич              |
| **Widgets**            | `features`, `entities`, `shared`            | Виджеты объединяют фичи и сущности                 |
| **Features**           | `entities`, `shared`                        | Фичи оперируют бизнес-сущностями                   |
| **Entities**           | `entities`, `shared`                        | Бизнес-сущности могут использовать шаред код       |
| **Shared**             | `shared`                                    | Базовый слой, не зависит ни от кого                |

> ❌ **Запрещено:** Например, `features` НЕ может импортировать `widgets` или `pages`. Слой не может знать о том, что находится выше него.

---

## 3. Public API и Инкапсуляция 🔒

### Правило Cross-Imports (Внутри одного слоя)

В классическом FSD импорты между слайсами одного слоя (например, `features/auth` -> `features/chat`) **запрещены**. Это нужно для высокой связности (cohesion) внутри слайса и низкой связности (coupling) между ними.

Однако, в реальных проектах иногда возникают ситуации, когда фичам нужно общаться. Для этого мы используем **Cross-Imports через `@x`**.

- **Как это работает:** Если вам нужно экспортировать что-то для другого слайса того же уровня, поместите это в папку `@x` внутри слайса (или экспортируйте через путь, содержащий `@x`).
- **Синтаксис:** `import { something } from "@features/another-feature/@x/something";`
- **Правило линтера:** Разрешает импорт, если путь содержит `/@x/`.

> ⚠️ **Важно:** Используйте это только в крайних случаях. Если слайсы слишком сильно зависят друг от друга, возможно, их стоит объединить или вынести общую логику в `shared` / `entities`.

### Исключения для слоя App 🏗️

Обычно слой `App` является самым верхним и ничего не экспортирует. Однако в нашем конфиге сделано **исключение**:

1.  **Всем слоям разрешено импортировать из `App`**.
    - **Зачем:** Это часто необходимо для доступа к глобальным типам (`RootState`, `AppDispatch`), конфигурации окружения или глобальным провайдерам, которые инициализируются в `app`.

2.  **Слой `App` может экспортировать внутренние файлы напрямую**.
    - Правило `boundaries/entry-point` разрешает прямой доступ к `app/**/ui/**`, `app/**/model/**` и т.д.
    - **Зачем:** В основном это нужно для настройки роутинга (`providers/router`), где нам необходимо лениво подгружать страницы или компоненты, лежащие глубоко в структуре приложения, или использовать фабрики провайдеров.

---

## 4. Запрещенные импорты (Restricted Imports) 🚫

Дополнительные жесткие правила для предотвращения спагетти-кода:

- **Shared Config/Lib/Hooks:** Все должно импортироваться только через `index.ts`.
- **UI Components:** Нельзя импортировать конкретный файл компонента, только через индекс папки UI.
- **API & Model:** Сервисы и модели должны быть доступны только через публичный интерфейс слайса.

---

## Решение проблем

Если линтер ругается:

1.  **Ошибка сортировки:** Запустите `bun run lint -- --fix` (или настройте авто-фикс при сохранении).
2.  **Ошибка границ (Boundaries):** Проверьте, не пытаетесь ли вы импортировать верхний слой в нижний (например, виджет в фичу). Если архитектурно это необходимо, возможно, ваш код должен лежать выше (в виджетах) или ниже (в shared).
3.  **Ошибка Public API:** Проверьте импорт. Вместо `path/to/slice/internal/file` используйте `path/to/slice`. Если нужного экспорта нет, добавьте его в `index.ts` этого слайса.
